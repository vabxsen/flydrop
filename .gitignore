*.iml
.gradle/
local.properties
.idea/
.vscode/
.DS_Store
build/
captures/
.externalNativeBuild/
.cxx/

# Firebase config: kept out of this public repo. The Google Services plugin is
# applied conditionally in app/build.gradle.kts, so the project still builds
# without it - sign-in simply reports that it is not configured.
app/google-services.json

# Release signing. Never commit these: the keystore is the app's permanent
# identity and the properties file holds its passwords.
*.jks
*.keystore
keystore.properties
dist/
